//! Shared HTTP DTOs between the `ananke` daemon and the `anankectl` CLI.
//!
//! No business logic lives here — only serde-friendly types that appear on
//! the wire. Task 2 fleshes out the real types.

pub const _CRATE_READY: () = ();
