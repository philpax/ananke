//! `DELETE /api/oneshot/{id}` — delete a oneshot.
//!
//! Returns `204 No Content` on success (no body).

// The DELETE endpoint has no bespoke request or response body type.
// This module exists so the per-endpoint module structure is uniform.
