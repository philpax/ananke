[project]
name = "ananke-stress"
version = "0.1.0"
description = "Stress-test scenarios for a running ananke daemon"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "tomlkit>=0.13",
    "websockets>=12",
]

[tool.uv]
# Lockfile kept next to pyproject.toml.
